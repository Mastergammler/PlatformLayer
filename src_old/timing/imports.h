#pragma once

// NOTE: i actually only need <winnt.h> but this produces a
// 'unknown type CONST' error
#include <windows.h>

#include <iomanip>
#include <sstream>
#include <string>

using std::setprecision;
using std::stringstream;
